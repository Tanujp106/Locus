# Security Policy

Review every hook, script, MCP server, and external plugin source before enabling it. Treat integrations as executable code with access to the host environment or external services.

Do not commit API keys, OAuth tokens, private memory, local runtime state, or generated transcripts containing sensitive data.

For a suspected security issue, keep the details private until the issue can be reviewed and remediated.
